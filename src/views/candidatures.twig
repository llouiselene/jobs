<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Candidatures</title>
    <link rel="stylesheet" href="/dev/assets/style.css">
    <link crossorigin="anonymous" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
          integrity="sha512-9usAa10IRO0HhonpyAIVpjrylPvoDwiPUiKdWk5t3PyolY1cOd4DSE0Ga+ri4AuTroPR5aQvXU9xC6qOPnzFeg=="
          referrerpolicy="no-referrer" rel="stylesheet"/>
</head>
<body>
{% include 'menu.twig' %}

<hr class="hr-large">

<div class="title-and-search">
    <div class="Title-box">Candidature</div>
    <div class="search-container">
        <div class="search-icon-container" id="searchIcon">
            <i class="fas fa-search" style="color: white;"></i>
        </div>
    </div>
</div>

<input type="text" id="searchInput" class="search-input" placeholder="Rechercher...">

<div class="space2-5"></div>

<div class="Title"> Liste des Candidatures :</div>

<div class="space5"></div>

<div class="job-listing">
    <div class="vertical-scroll-bar">
        {% for candidature in candidatures %}
            <a class="job-box" href="?uri=detail_offre&id={{ candidature.Id_Offre }}">
                <h2>{{ candidature.Nom_Offre }}</h2>
                <p>Entreprise: {{ candidature.Id_Offre }}</p>
                <p>Date: {{ candidature.Date_Candid }}</p>
                <p>Domaine: {{ candidature.Domaine_Offre }}</p>
                <div class="right-align">
                    <button class="button">État</button>
                </div>
            </a>
        {% else %}
            <p>Aucune candidature trouvée.</p>
        {% endfor %}

    </div>
</div>

<div class="space5"></div>

<div class="footer">
    <a href="?uri=mentions_legales">Mentions Légales</a> - 2025 Job* - À propos
</div>

<script src="/dev/assets/script.js"></script>
</body>
</html>
